name: RDP

on:
  workflow_dispatch:

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 3600

    steps:
      # -------------------------------------------------
      # Enable RDP and configure it to listen on port 4000
      # -------------------------------------------------
      - name: Configure Core RDP Settings
        run: |
          # Enable Remote Desktop
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                           -Name "fDenyTSConnections" -Value 0 -Force

          # Disable Network Level Authentication (optional)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "UserAuthentication" -Value 0 -Force

          # Set SecurityLayer to 0 (RDP Security)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "SecurityLayer" -Value 0 -Force

          # Change the listening port to 4000
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                           -Name "PortNumber" -Value 4000 -Force

          # Remove any existing firewall rule for the custom RDP/Tailscale rule
          netsh advfirewall firewall delete rule name="RDP‑Tailscale" >$null 2>&1

          # Open the new RDP port (4000) to inbound traffic
          netsh advfirewall firewall add rule name="RDP‑Tailscale" `
            dir=in action=allow protocol=TCP localport=4000

          # Restart the Remote Desktop service so the new port takes effect
          Restart-Service -Name TermService -Force

      # -------------------------------------------------
      # Create a dedicated RDP user with a strong password
      # -------------------------------------------------
      - name: Create RDP User with Secure Password
        run: |
          Add-Type -AssemblyName System.Security
          $charSet = @{
              Upper   = [char[]](65..90)
              Lower   = [char[]](97..122)
              Number  = [char[]](48..57)
              Special = ([char[]](33..47) + [char[]](58..64) +
                         [char[]](91..96) + [char[]](123..126))
          }
          $rawPassword = @()
          $rawPassword += $charSet.Upper | Get-Random -Count 4
          $rawPassword += $charSet.Lower | Get-Random -Count 4
          $rawPassword += $charSet.Number | Get-Random -Count 4
          $rawPassword += $charSet.Special | Get-Random -Count 4
          $password = -join ($rawPassword | Sort-Object { Get-Random })
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name "RDP" -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member "RDP"
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member "RDP"

          # Export credentials for later steps
          echo "RDP_CREDS=User: RDP | Password: $password" >> $env:GITHUB_ENV

          if (-not (Get-LocalUser -Name "RDP")) {
              Write-Error "User creation failed"
              exit 1
          }

      # -------------------------------------------------
      # Install Tailscale
      # -------------------------------------------------
      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-1.82.0-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      # -------------------------------------------------
      # Bring Tailscale online and capture its IP/port
      # -------------------------------------------------
      - name: Establish Tailscale Connection
        run: |
          # Bring up Tailscale with the auth key
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up `
            --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} `
            --hostname=gh-runner-$env:GITHUB_RUN_ID

          # Wait for an IP address to appear
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          if (-not $tsIP) {
              Write-Error "Tailscale IP not assigned. Exiting."
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV

          # Tailscale forwards all inbound ports, but we’ll expose the RDP port
          # using `tailscale serve` so the port number appears in the UI.
          & "$env:ProgramFiles\Tailscale\tailscale.exe" serve https://0.0.0.0:4000
          echo "TAILSCALE_RDP_PORT=4000" >> $env:GITHUB_ENV

      # -------------------------------------------------
      # Verify that the RDP port is reachable over Tailscale
      # -------------------------------------------------
      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          Write-Host "RDP port (Tailscale): $env:TAILSCALE_RDP_PORT"

          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP `
                                            -Port $env:TAILSCALE_RDP_PORT
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP connection to port $env:TAILSCALE_RDP_PORT failed"
              exit 1
          }
          Write-Host "TCP connectivity to port $env:TAILSCALE_RDP_PORT succeeded!"

      # -------------------------------------------------
      # Keep the runner alive and display connection details
      # -------------------------------------------------
      - name: Maintain Connection
        run: |
          Write-Host "`n=== RDP ACCESS ==="
          Write-Host "Address: $env:TAILSCALE_IP"
          Write-Host "Port: $env:TAILSCALE_RDP_PORT"
          Write-Host "Username: RDP"
          Write-Host "Password: $(echo $env:RDP_CREDS)"
          Write-Host "===================`n"

          # Keep the job alive (or until cancelled)
          while ($true) {
              Write-Host "[$(Get-Date)] RDP active on $env:TAILSCALE_IP:$env:TAILSCALE_RDP_PORT"
              Start-Sleep -Seconds 300
          }
